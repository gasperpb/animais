import React from 'react';

const Register = () => {
    const handleForm = (data) => {
        console.log(data);
    }
    return (<S.Container>
        
    </S.Container>
    );
};

export default Register;